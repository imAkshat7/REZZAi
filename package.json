{
  "name": "rezzai-backend",
  "version": "1.0.0",
  "description": "REZZAi Unified Backend Service",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm --prefix gateway install --legacy-peer-deps && npm --prefix services/auth install --legacy-peer-deps && npm --prefix services/chat install --legacy-peer-deps && npm --prefix services/agent install --legacy-peer-deps"
  },
  "dependencies": {
    "ioredis": "^6.0.0"
  }
}
